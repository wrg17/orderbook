services:
  orderbook-dev:
    image: orderbook-toolchain:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: toolchain
    volumes:
      - .:/app
    working_dir: /app
